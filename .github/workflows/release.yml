---
name: Release

# yamllint disable-line rule:truthy
on:
  push:
    branches:
      - main

jobs:
  default:
    # using a GitHub only release here. Use .github/update_workflows.sh to switch to a Docker release
    uses: Hapag-Lloyd/Workflow-Templates/.github/workflows/default_release_callable.yml@c815ffef37e1a8f85672cc59b15677bc93fa0e6d
    secrets: inherit
